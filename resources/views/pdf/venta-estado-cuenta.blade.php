<!doctype html><html lang="es"><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans;font-size:11px;color:#1f2937}h1{font-size:20px}table{width:100%;border-collapse:collapse;margin-top:14px}th,td{border:1px solid #d1d5db;padding:6px;text-align:left}.right{text-align:right}</style></head><body>
<h1>{{ $settings['system_name'] ?? 'Hogar Inmobiliaria' }} — Estado de cuenta</h1>
<p>Generado: {{ now()->format('d/m/Y H:i') }}</p><p><strong>Cliente:</strong> {{ $venta->cliente->nombre }}</p>
<p><strong>Terreno:</strong> {{ $venta->lote->manzano->urbanizacion->nombre }} / Mz {{ $venta->lote->manzano->codigo }} / Lote {{ $venta->lote->codigo }}</p>
<p><strong>Modalidad:</strong> {{ $venta->tipo_operacion }} · <strong>Precio pactado:</strong> {{ number_format((float)$venta->precio_final,2) }} · <strong>Total pagado:</strong> {{ number_format((float)$totalPagado,2) }} · <strong>Saldo actual:</strong> {{ number_format((float)$saldo,2) }}</p>
<h2>Plan de cuotas</h2><table><thead><tr><th>N.º</th><th>Vencimiento</th><th>Monto</th><th>Pagado</th><th>Saldo</th><th>Estado</th></tr></thead><tbody>@foreach($venta->cuotas as $cuota)<tr><td>{{ $cuota->numero }}</td><td>{{ $cuota->fecha_vencimiento?->format('d/m/Y') }}</td><td class="right">{{ number_format((float)$cuota->monto,2) }}</td><td class="right">{{ number_format((float)$cuota->monto_pagado,2) }}</td><td class="right">{{ number_format((float)$cuota->saldo_pendiente,2) }}</td><td>{{ $cuota->estado }}</td></tr>@endforeach</tbody></table>
<h2>Pagos</h2><table><thead><tr><th>Fecha</th><th>Concepto</th><th>Método</th><th>Referencia</th><th>Monto</th><th>Estado</th></tr></thead><tbody>@foreach($venta->cashMovements->where('tipo','ingreso') as $pago)<tr><td>{{ $pago->fecha?->format('d/m/Y') }}</td><td>{{ $pago->concepto }}</td><td>{{ $pago->metodo_pago }}</td><td>{{ $pago->referencia }}</td><td class="right">{{ number_format((float)$pago->monto,2) }}</td><td>{{ $pago->estado }}</td></tr>@endforeach</tbody></table>
</body></html>
