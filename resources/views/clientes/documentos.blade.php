@extends('layouts.app')
@section('content')
<div class="topbar"><h1 class="title">Documentos del terreno</h1><a class="btn secondary" href="{{ route('portal.terrenos.show',$venta) }}">Volver</a></div><div class="card"><h2>{{ $venta->lote->manzano->urbanizacion->nombre }} / Mz {{ $venta->lote->manzano->codigo }} / Lote {{ $venta->lote->codigo }}</h2><div class="actions"><a class="btn" href="{{ route('portal.contrato',$venta) }}">Contrato</a><a class="btn" href="{{ route('portal.plan',$venta) }}">Plan de pagos</a><a class="btn" href="{{ route('portal.estado-cuenta.pdf',$venta) }}">Estado de cuenta</a></div></div><div class="card" style="margin-top:18px"><h2>Recibos confirmados</h2>@forelse($venta->cashMovements->where('tipo','ingreso')->where('estado','confirmado') as $pago)<p>{{ $pago->fecha?->format('d/m/Y') }} · {{ number_format((float)$pago->monto,2) }} · <a href="{{ route('portal.recibo',$pago) }}">Descargar recibo</a></p>@empty<p>Sin recibos confirmados.</p>@endforelse</div>
@endsection
