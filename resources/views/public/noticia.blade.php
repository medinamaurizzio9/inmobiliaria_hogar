<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $noticia->titulo }}</title><meta name="description" content="{{ $noticia->resumen }}"><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head><body class="premium-public"><main class="public-article"><a href="{{ route('login') }}#noticias">← Volver a noticias</a>@if($noticia->imagen)<img src="{{ Storage::disk('public')->url($noticia->imagen) }}" alt="" fetchpriority="high">@endif<time>{{ $noticia->fecha_publicacion->format('d/m/Y') }}</time><h1>{{ $noticia->titulo }}</h1><p class="lead">{{ $noticia->resumen }}</p><div class="article-content">{!! nl2br(e($noticia->contenido)) !!}</div>@if($recientes->isNotEmpty())<h2>Noticias recientes</h2>@foreach($recientes as $item)<p><a href="{{ route('public.noticias.show',$item->slug) }}">{{ $item->titulo }}</a></p>@endforeach @endif</main></body></html>
