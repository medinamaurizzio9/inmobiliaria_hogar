<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $noticia->titulo }} · {{ $systemSettings['system_name'] }}</title><meta name="description" content="{{ $noticia->resumen }}">@include('partials.pwa-head')<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head><body class="premium-public"><main class="public-article"><a href="{{ route('login') }}#noticias">← Volver a noticias</a>@if($noticia->imageUrl())<img src="{{ $noticia->imageUrl() }}" alt="{{ $noticia->titulo }}" fetchpriority="high">@else<div class="public-article-placeholder"><i class="fa-regular fa-newspaper"></i></div>@endif<time>{{ $noticia->fecha_publicacion->format('d/m/Y') }}</time><h1>{{ $noticia->titulo }}</h1><p class="lead">{{ $noticia->resumen }}</p><div class="article-content">{!! nl2br(e($noticia->contenido)) !!}</div>@if($recientes->isNotEmpty())<h2>Noticias recientes</h2>@foreach($recientes as $item)<p><a href="{{ route('public.noticias.show', $item->slug) }}">{{ $item->titulo }}</a></p>@endforeach @endif</main><script src="{{ asset('js/pwa.js') }}"></script></body></html>
